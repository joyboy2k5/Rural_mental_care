# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey

VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini Service Identity (SI) key
# Some deployment or server-side setups use a separate SI key or signed-in service
# credential. If you need this, set it here. Leave blank if not used.
VITE_GEMINI_SI_KEY=your_gemini_si_key_here
